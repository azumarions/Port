import React from "react";
import { useSelector, useDispatch } from "react-redux";
import { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
    </div>
  );
};

export default Home;